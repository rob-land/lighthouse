Source: lighthouse
Section: utils
Priority: optional
Maintainer: Rob Land <thick.beach7895@fastmail.com>
Build-Depends:
 debhelper-compat (= 13),
 dh-python,
 meson (>= 1.0.0),
 ninja-build,
 blueprint-compiler,
 gettext,
 desktop-file-utils,
 appstream,
 pkg-config,
 libglib2.0-bin,
 libglib2.0-dev,
 libglib2.0-dev-bin,
 libgtk-4-bin,
 python3-all,
 python3-gi,
 python3-cryptography,
 gir1.2-glib-2.0,
 gir1.2-gtk-4.0,
 gir1.2-adw-1,
 glib-networking,
Standards-Version: 4.7.0
Homepage: https://github.com/rob-land/lighthouse
Rules-Requires-Root: no

Package: lighthouse
Architecture: all
Depends:
 ${misc:Depends},
 ${python3:Depends},
 python3-gi,
 python3-gi-cairo,
 python3-cryptography,
 gir1.2-gtk-4.0,
 gir1.2-adw-1,
 gir1.2-gstreamer-1.0,
 gstreamer1.0-plugins-base,
 gstreamer1.0-plugins-good,
 glib-networking,
Recommends:
 wireplumber,
 avahi-daemon,
 gir1.2-lfb-0.0,
Suggests:
 kdeconnect,
Description: find-my-device agent and viewer for Linux phones
 Lighthouse makes a Phosh device findable across several reach tiers and
 renders the result natively, so Android and Linux phones appear in one
 GTK viewer.
 .
 It is a node speaking existing protocols rather than a from-scratch
 stack: it answers KDE Connect findmyphone pages on the LAN over a
 mutually-authenticated, certificate-pinned TLS link, so a ring can be
 triggered from stock KDE Connect, Valent or Plasma.
 .
 A page raises a full-screen takeover surface that rings past silent mode
 — the tone is a raw PipeWire stream rather than a feedbackd event, and
 the default sink is unmuted and raised for the duration, then restored.
 .
 This package installs the agent as a systemd user unit so the device
 stays findable without the viewer running.
